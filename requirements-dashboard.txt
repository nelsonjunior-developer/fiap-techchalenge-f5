-r requirements.txt
evidently==0.4.36
# Dashboard app (Streamlit) should run in an isolated venv to avoid local protobuf conflicts.
# Pinned to improve local reproducibility for the dashboard stack.
streamlit==1.39.0
